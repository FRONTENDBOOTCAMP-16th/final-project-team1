[Slide 1] 표지: 프로젝트 타이틀
화면 구성: [체크메이트] 로고 및 메인 로그인 화면

텍스트 내용:

메인: 스마트 통합 출결 관리 시스템, 체크메이트(Checkmate)

서브: 체스에서 승부를 결정짓는 확실한 한 수처럼, 번거로운 출결 업무를 완벽하게 끝내다.

정보: 팀 1 / 김한결, 백희연, 윤유영, 이주연, 정인우 / 개발 기간 (26.04.16 ~ 05.19) / 발표자: 정인우

[Slide 2] 프로젝트 개요 및 기획
화면 구성: 학생과 관리자가 상호작용하는 직관적인 도식화

텍스트 내용:

목표: 기존 수기 출결의 문제점을 디지털로 완벽히 전환

타겟: 자기주도적 관리가 필요한 '학생' & 통합 제어가 필요한 '관리자'

특징: 철저한 권한 분리 및 PC(1920x1080) 최적화 맞춤형 UI 제공

[Slide 4] 기술 스택 (Tech Stack)
화면 구성: 프론트엔드 스택 로고(React, Zustand 등)를 중앙에 크게 배치, 하단 모서리에 백엔드 스택(Spring Boot 등) 작게 배치
or 백엔드 스택을 보여준 후 프론트엔드 스택으로 이동.

텍스트 내용:
Frontend
Core: React, TypeScript, Vite, Standard CSS
Server State & API: TanStack Query, Axios
Client State: Zustand
Form & Validation: React Hook Form, Zod
Data Visualization: Recharts

Backend
Language Java 17
Framework Spring Boot 3.5.14
Database MySQL (Aiven Cloud)
ORM Spring Data JPA
인증 Spring Security + JWT
API 문서 Swagger (SpringDoc OpenAPI)
빌드 Gradle
배포 Docker

첫째, React와 TypeScript, 그리고 Vite의 조합입니다.
단순히 유행해서가 아닙니다. 관리자와 학생이라는 두 가지 뷰를 컴포넌트 단위로 재사용하기 위해 React를,
런타임 단계에서 터질 수 있는 치명적인 에러를 컴파일 단계에서 미리 잡기 위해 TypeScript를 엄격하게 적용했습니다.
빌드와 HMR 속도가 압도적인 Vite는 한 달이라는 짧은 개발 기간 동안 팀의 생산성을 극대화해 주었습니다.

둘째, 가장 고민이 많았던 '상태 관리'의 분리입니다.
저희는 상태를 두 가지로 나누었습니다.
서버에서 가져오는 데이터(출결 내역, 공지사항 등)는 TanStack Query를 도입해 관리했습니다.
이를 통해 불필요한 API 중복 호출을 캐싱(Caching)으로 막아 서버 부하를 줄이고, 로딩 및 에러 상태를 선언적으로 처리했습니다.
반면, 클라이언트에서만 쓰이는 상태(검색 조건, 사이드바, 공통 모달 등)는 무거운 Redux 대신
가볍고 직관적인 Zustand를 사용해 프론트엔드 전역 상태를 깔끔하게 분리했습니다.

셋째, Form 핸들링과 유효성 검사 최적화입니다.
로그인, 휴가 신청, 학생 등록 등 폼 입력이 많은 시스템 특성상,
입력할 때마다 화면이 리렌더링되는 제어 컴포넌트의 단점을 극복해야 했습니다.
그래서 비제어 컴포넌트 기반의 React Hook Form을 도입해 렌더링 성능을 끌어올렸고,
Zod를 결합하여 비밀번호 정책이나 필수 입력값 검증을 스키마 형태로 단단하게 구현했습니다.

넷째, 직관적인 데이터 시각화입니다.
관리자 대시보드에서 수많은 출결 통계를 단순히 표로 보여주는 것은 UX 관점에서 좋지 않다고 판단했습니다.
이에 React 생태계와 가장 잘 어울리고 커스터마이징이 유연한 Recharts를 도입하여,
데이터를 한눈에 파악할 수 있는 대시보드를 완성했습니다.

마지막으로, 스타일링 컨벤션입니다.
저희는 CSS 컴포넌트 폴더 구조를 체계화했기 때문에,
직관적인 클래스명을 통한 공통 스타일 관리로 팀원 간의 퍼블리싱 협업이 굉장히 효율적이었습니다.

[Slide 5] 인증 및 로그인(Auth)
화면 구성: 로그인 화면 캡처 + JWT 라우팅 플로우 차트

텍스트 내용:

보안 및 라우팅: 단일 진입점(/) 로그인 ➔ JWT 디코딩 ➔ 즉각적인 권한(Role) 분리

보안 강화: 로그아웃 시 localStorage.clear()를 통한 로컬 데이터 완벽 파기

[Slide 6] 페이지 뷰 1: 학생 서비스 (Student)
화면 구성: 2분할 슬라이드 2개

대시보드 : 실시간 출석률 및 핵심 정보 요약

휴가 신청 : 직관적인 캘린더 연동 및 진행 상태 트래킹

공지사항 : 페이지네이션 적용 및 상태별(공개/비공개) 게시물 조회

설정 : 사용자 정보 확인 및 비밀번호 변경

[Slide 7] 페이지 뷰 2:관리자 서비스 (admin)
화면 구성: 2분할 슬라이드 3개 (관리자 설정은 학생 설정과 비슷해서 넘어가거나 학생 페이지 뷰 마지막에 같이 설명)

대시보드 : 전체 학생의 실시간 상태(출석, 지각, 결석)를 Recharts로 시각화하여 한눈에 파악.

학생 관리 : 전체 학생 명단 조회 및 필터링 검색(이름, 학번, 강의명). 신규 학생 등록 및 정보 수정(CRUD).

출결 관리 : 날짜별/상태별 상세 필터링. 특정 학생의 입/퇴실 시간을 수동으로 수정하거나 승인 가능.

강의 관리 : 기수별 강의명, 시작일, 종료일 관리. 강의 상태(진행 중, 수료) 실시간 업데이트.

휴가 승인 : 학생들의 휴가 신청 내역을 '승인 대기' 상태별로 모아보기. 클릭 한 번으로 승인/반려 즉각 처리.

공지사항 관리 : 전체 공지사항 리스트 관리. 특정 게시글의 '공개/비공개' 상태 토글 및 에디터 연동.

관리자 설정 : 관리자 프로필 확인 및 보안을 위한 주기적인 비밀번호 변경 기능.

[Slide 8] 협업 및 WBS 마일스톤
화면 구성: 한 달짜리 WBS 일정표 캡처 이미지

Notion, Discord, GitHub 기반의 투명한 소통 및 API 명세 조율

화면 구성: 캔바에서 V자 모양으로 도형 배치
좌측 하향 (설계 파트):
시스템 설계 (System Design): 화면 구조 설계, ERD, 프론트엔드 아키텍처
상세 설계 (Detailed Design): UI 시안(Figma), API 명세서 작성

최하단 (구현):
구현 (Implementation): React & Spring Boot 개발

우측 상향 (테스트 파트):
단위 테스트 (Unit Test): 개별 컴포넌트 동작, API 단일 호출 및 에러 핸들링 점검
통합 테스트 (Integration Test): 프론트엔드-백엔드 데이터 연동 및 전체 페이지 흐름(라우팅) 검증

[Slide 9] 시연영상
